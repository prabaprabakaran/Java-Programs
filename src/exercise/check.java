package exercise;

public class check {
public static void main(String[] args) {
	int x=2;
    int y1=3;
    int i=0;
    int s=0;
    int k=0;
    System.out.printf( "x = %d\n", x ); 
    System.out.printf( "Value of %d + %d is %d\n", x, x, ( x + x ) ); 
    System.out.printf( "x =" ); 
    System.out.printf( "%d = %d\n", ( x + y1), ( y1 + x ) );
    int p=i+s+k+7; 
    int y=0;
    int a=1;
    System.out.println( "variables whose values are modified" ); 
    System.out.println( "a = 5"); 
    
    y=a*x*x*x+7;
    System.out.println((a*x)*x*x+7);
    y=a*x*x*(x+7 ); 
    y=(a*x)*x*(x+7 ); 
    y=(a*x)*x*x+7; 
    y=a*(x*x*x)+7; 
    y=a*x*(x*x+7 );
System.out.println(x=7 + 3 * 6 / 2 - 1); 
    x=2 % 2 + 2 * 2 - 2 / 2; 
    x=(3 * 9 *(3 +(9 * 3 /(3 ) ) ) ); 
}
}
